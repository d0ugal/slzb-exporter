name: Auto Format

on:
  push:
  pull_request:
    paths-ignore:
      - CHANGELOG.md

jobs:
  call-auto-format:
    uses: d0ugal/promexporter/.github/workflows/common/auto-format.yml@v1.14.0
    secrets: inherit
